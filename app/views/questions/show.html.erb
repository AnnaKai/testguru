<!-- todo-review: do we actually need that Show? Index is enough. Remove later? !-->
<div class="container py-5">
  <div class="row">
    <div class="offset-md-3 col-md-6">
      <h2 class="text-center mb-4">Question - <%= @question.body %></h2>
      <div>
        <%= link_to 'Edit', edit_question_path(@question), :class => "btn btn-primary mr-3" %>
        <%= link_to 'Delete', question_path(@question.id), method: :delete, :class => "btn btn-danger"%>
      </div>
    </div>
  </div>
</div>
